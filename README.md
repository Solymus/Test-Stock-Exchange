# Demo Stock Exchange 

This is simple Exchange used for Crypto-Stock Protocol testings.


### Unit testing

Use Rust testing: `cargo test`

### Integration test Sample

1. Run local Rust server `cargo run`
2. Make sure you installed Python and installed `requests` lib
3. Run integration test `python src/tests/integration_test.py`